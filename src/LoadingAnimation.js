import React from 'react';
import ReactLoading from 'react-loading';

const LoadingAnimation = () => (
    <ReactLoading type={'spinningBubbles'} color={'#0765fc'} height={30} width={30} />
);

export default LoadingAnimation;